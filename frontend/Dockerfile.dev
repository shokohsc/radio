# build stage
FROM node:20-alpine AS build-stage
WORKDIR /app
COPY package*.json ./
RUN apk add --no-cache bash gettext ca-certificates && \
    npm ci && npm cache clean --force
COPY . .

COPY ./start-caddy.sh /usr/bin/start-caddy.sh
RUN chmod +x /usr/bin/start-caddy.sh

# Create non-root user for dev
RUN addgroup -S radio && adduser -S radio -G radio && \
    chown -R radio:radio /app /usr/bin/start-caddy.sh
USER radio

ENTRYPOINT [ "start-caddy.sh" ]
CMD ["dev"]
